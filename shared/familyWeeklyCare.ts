export function getWeekKey(date = new Date()): string { const day = date.getDay() || 7; const monday = new Date(date); monday.setDate(date.getDate() - day + 1); return `${monday.getFullYear()}-${String(monday.getMonth() + 1).padStart(2, "0")}-${String(monday.getDate()).padStart(2, "0")}`; }
export function countAvailableSupporters(profiles: Array<{ strengths: string[] }>): number { return profiles.filter((profile) => profile.strengths.length > 0).length; }
export function getReminderTone(dueAt?: Date | null): "gentle" | "soon" | "today" { if (!dueAt) return "gentle"; const now = new Date(); const due = new Date(dueAt); const dayGap = Math.floor((new Date(due.getFullYear(), due.getMonth(), due.getDate()).getTime() - new Date(now.getFullYear(), now.getMonth(), now.getDate()).getTime()) / 86_400_000); return dayGap <= 0 ? "today" : dayGap <= 2 ? "soon" : "gentle"; }
