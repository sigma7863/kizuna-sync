import { CalendarDays } from "lucide-react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
export function FamilyWeekendPlanner({ familyGroupId }: { familyGroupId: number }) { const {data=[]}=trpc.weekendPlanner.suggestions.useQuery({familyGroupId}); const poll=trpc.familyPoll.create.useMutation(); return <Card className="border-0 bg-emerald-50 shadow-md"><CardHeader className="pb-2"><CardTitle className="flex gap-2 text-base"><CalendarDays className="h-5 w-5 text-emerald-600"/>家族の週末プランナー</CardTitle></CardHeader><CardContent className="grid gap-2">{data.map(x=><div key={x.id} className="rounded-xl bg-white p-3"><span className="text-[10px] text-emerald-700">{x.type}</span><p className="text-sm font-semibold">{x.title}</p><p className="text-xs text-slate-500">{x.description}</p><Button size="sm" className="mt-2" onClick={()=>poll.mutate({familyGroupId,question:`イベント：週末は何をする？`,options:[x.title,"別の案を選ぶ"],endsAt:new Date(Date.now()+2*86400000).toISOString()})}>家族に提案</Button></div>)}</CardContent></Card> }
