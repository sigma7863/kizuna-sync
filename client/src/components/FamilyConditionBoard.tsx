import { Activity, Footprints, Heart, Loader2, MapPin, ShieldCheck, Users } from "lucide-react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { trpc } from "@/lib/trpc";

export function FamilyConditionBoard({ familyGroupId }: { familyGroupId: number }) {
  const { data, isLoading } = trpc.highlights.today.useQuery({ familyGroupId }, { enabled: familyGroupId > 0, refetchInterval: 60_000 });
  return <Card className="border-0 bg-gradient-to-br from-teal-50 via-white to-emerald-50 shadow-md"><CardHeader className="pb-2"><CardTitle className="flex items-center gap-2 text-base text-slate-800"><Activity className="h-5 w-5 text-teal-600" />家族コンディションボード</CardTitle><p className="text-xs text-slate-500">見守り情報を、必要な分だけやさしく確認できます。</p></CardHeader><CardContent>{isLoading ? <div className="flex min-h-28 items-center justify-center text-xs text-slate-500"><Loader2 className="mr-2 h-4 w-4 animate-spin" />更新中…</div> : <><div className="grid grid-cols-3 gap-2"><div className="rounded-2xl bg-teal-50 p-3 text-center"><Users className="mx-auto h-4 w-4 text-teal-600" /><p className="mt-1 text-lg font-bold text-slate-800">{data?.activeMemberCount ?? 0}</p><p className="text-[10px] text-slate-500">活動中</p></div><div className="rounded-2xl bg-sky-50 p-3 text-center"><MapPin className="mx-auto h-4 w-4 text-sky-600" /><p className="mt-1 text-lg font-bold text-slate-800">{data?.locationCount ?? 0}</p><p className="text-[10px] text-slate-500">見守り地点</p></div><div className="rounded-2xl bg-rose-50 p-3 text-center"><Footprints className="mx-auto h-4 w-4 text-rose-500" /><p className="mt-1 text-lg font-bold text-slate-800">{((data?.totalSteps ?? 0) / 1000).toFixed(1)}k</p><p className="text-[10px] text-slate-500">家族の歩数</p></div></div><p className="mt-3 flex items-center gap-1.5 rounded-xl bg-white/85 p-2 text-xs text-teal-800"><ShieldCheck className="h-4 w-4" />見守りは、安心のために必要な情報だけを共有しています。</p></>}</CardContent></Card>;
}
