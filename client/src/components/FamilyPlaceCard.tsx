import { useState } from "react";
import { CheckCircle2, Loader2, MapPinned, Plus } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";

export function FamilyPlaceCard({ familyGroupId }: { familyGroupId: number }) { const utils = trpc.useUtils(); const [placeName, setPlaceName] = useState(""); const [reason, setReason] = useState(""); const { data: places = [], isLoading } = trpc.placeCards.list.useQuery({ familyGroupId }, { enabled: familyGroupId > 0 }); const create = trpc.placeCards.create.useMutation({ onSuccess: async () => { setPlaceName(""); setReason(""); await utils.placeCards.list.invalidate({ familyGroupId }); } }); const update = trpc.placeCards.update.useMutation({ onSuccess: () => utils.placeCards.list.invalidate({ familyGroupId }) }); return <Card className="border-0 bg-gradient-to-br from-emerald-50 via-white to-lime-50 shadow-md"><CardHeader className="pb-2"><CardTitle className="flex items-center gap-2 text-base"><MapPinned className="h-5 w-5 text-emerald-700"/>家族の行ってみたい場所カード</CardTitle><p className="text-xs text-slate-500">次の休日に寄ってみたい場所やイベントを集めよう。</p></CardHeader><CardContent className="space-y-3"><div className="grid gap-2 rounded-xl bg-white/80 p-3"><Input value={placeName} onChange={(event) => setPlaceName(event.target.value)} placeholder="例：駅前の新しい図書館" maxLength={160}/><div className="flex gap-2"><Input value={reason} onChange={(event) => setReason(event.target.value)} placeholder="行ってみたい理由（任意）" maxLength={220}/><Button size="sm" disabled={!placeName.trim() || create.isPending} onClick={() => create.mutate({ familyGroupId, placeName, reason })}>{create.isPending ? <Loader2 className="h-4 w-4 animate-spin"/> : <><Plus className="mr-1 h-4 w-4"/>追加</>}</Button></div></div>{isLoading ? <p className="py-3 text-center text-xs text-emerald-700">場所カードを読み込み中です…</p> : <div className="grid gap-2">{places.map((place) => <article key={place.id} className={`rounded-xl p-3 shadow-sm ${place.isVisited ? "bg-emerald-50" : "bg-white"}`}><div className="flex items-start justify-between gap-2"><div><p className={`text-sm ${place.isVisited ? "text-emerald-700 line-through" : "font-semibold text-slate-800"}`}>{place.placeName}</p>{place.reason && <p className="mt-1 text-xs text-emerald-800">{place.reason}</p>}</div><button type="button" aria-label={place.isVisited ? "未訪問に戻す" : "行けた"} onClick={() => update.mutate({ familyGroupId, placeId: place.id, isVisited: !place.isVisited })} className={place.isVisited ? "text-emerald-600" : "text-slate-400"}><CheckCircle2 className="h-5 w-5"/></button></div></article>)}{places.length === 0 && <p className="rounded-xl border border-dashed border-emerald-200 p-3 text-center text-xs text-slate-500">休日に寄ってみたい場所を、一つ貯めてみましょう。</p>}</div>}</CardContent></Card>; }
