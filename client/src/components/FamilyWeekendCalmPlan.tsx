import { useState } from "react";
import { CheckCircle2, Coffee, Loader2, Plus } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";

export function FamilyWeekendCalmPlan({ familyGroupId }: { familyGroupId: number }) { const utils = trpc.useUtils(); const [plan, setPlan] = useState(""); const [timingHint, setTimingHint] = useState(""); const { data: plans = [], isLoading } = trpc.weekendCalmPlans.list.useQuery({ familyGroupId }, { enabled: familyGroupId > 0 }); const create = trpc.weekendCalmPlans.create.useMutation({ onSuccess: async () => { setPlan(""); setTimingHint(""); await utils.weekendCalmPlans.list.invalidate({ familyGroupId }); } }); const update = trpc.weekendCalmPlans.update.useMutation({ onSuccess: () => utils.weekendCalmPlans.list.invalidate({ familyGroupId }) }); return <Card className="border-0 bg-gradient-to-br from-emerald-50 via-white to-teal-50 shadow-md"><CardHeader className="pb-2"><CardTitle className="flex items-center gap-2 text-base"><Coffee className="h-5 w-5 text-emerald-700"/>家族の週末ほっと計画</CardTitle><p className="text-xs text-slate-500">週末に休める時間や気分転換の予定を、ゆるく共有しよう。</p></CardHeader><CardContent className="space-y-3"><div className="grid gap-2 rounded-xl bg-white/80 p-3"><Input value={plan} onChange={(event) => setPlan(event.target.value)} placeholder="例：日曜の午後にゆっくり散歩" maxLength={180}/><div className="flex gap-2"><Input value={timingHint} onChange={(event) => setTimingHint(event.target.value)} placeholder="いつごろ？（任意）" maxLength={120}/><Button size="sm" disabled={!plan.trim() || create.isPending} onClick={() => create.mutate({ familyGroupId, plan, timingHint })}>{create.isPending ? <Loader2 className="h-4 w-4 animate-spin"/> : <><Plus className="mr-1 h-4 w-4"/>入れる</>}</Button></div></div>{isLoading ? <p className="py-3 text-center text-xs text-emerald-700">週末の計画を読み込み中です…</p> : <div className="grid gap-2">{plans.map((entry) => <article key={entry.id} className={`rounded-xl p-3 shadow-sm ${entry.isEnjoyed ? "bg-emerald-50" : "bg-white"}`}><div className="flex items-start justify-between gap-2"><div><p className={`text-sm ${entry.isEnjoyed ? "text-emerald-700 line-through" : "font-semibold text-slate-800"}`}>{entry.plan}</p>{entry.timingHint && <p className="mt-1 text-xs text-emerald-800">{entry.timingHint}</p>}</div><button type="button" aria-label={entry.isEnjoyed ? "未実施に戻す" : "楽しめた"} onClick={() => update.mutate({ familyGroupId, planId: entry.id, isEnjoyed: !entry.isEnjoyed })} className={entry.isEnjoyed ? "text-emerald-600" : "text-slate-400"}><CheckCircle2 className="h-5 w-5"/></button></div></article>)}{plans.length === 0 && <p className="rounded-xl border border-dashed border-emerald-200 p-3 text-center text-xs text-slate-500">今週末、少し休めそうな時間を一つだけ予定にしてみましょう。</p>}</div>}</CardContent></Card>; }
