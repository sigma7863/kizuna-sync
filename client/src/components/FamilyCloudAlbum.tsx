import { useRef, useState } from "react";
import { Download, Heart, ImagePlus, Loader2, MessageCircleHeart, Search, Sparkles, Tag, X } from "lucide-react";
import { toast } from "sonner";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { useI18n } from "@/contexts/I18nContext";
import { trpc } from "@/lib/trpc";
import { MAX_ALBUM_PHOTO_BYTES, isSupportedAlbumMimeType, type AlbumMimeType } from "@shared/album";
import { getFamilyAlbumDiscoveryMode } from "@shared/familyAlbumDiscovery";

type FamilyCloudAlbumProps = { familyGroupId: number };

function readAsDataUrl(file: File) { return new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onerror = () => reject(new Error("image-read-failed")); reader.onload = () => resolve(String(reader.result)); reader.readAsDataURL(file); }); }

export function FamilyCloudAlbum({ familyGroupId }: FamilyCloudAlbumProps) {
  const { t } = useI18n();
  const [favoritesOnly, setFavoritesOnly] = useState(false);
  const [keyword, setKeyword] = useState("");
  const [statusMessage, setStatusMessage] = useState("");
  const fileInputRef = useRef<HTMLInputElement | null>(null);
  const utils = trpc.useUtils();
  const { data: photos = [], isLoading } = trpc.album.list.useQuery({ familyGroupId, favoritesOnly }, { enabled: familyGroupId > 0 });
  const { data: searchedPhotos = [], isLoading: isSearching } = trpc.album.search.useQuery({ familyGroupId, keyword }, { enabled: familyGroupId > 0 && keyword.trim().length > 0 });
  const displayedPhotos = keyword.trim() ? searchedPhotos : photos;
  const discoveryMode = getFamilyAlbumDiscoveryMode(keyword, favoritesOnly);
  const uploadPhoto = trpc.album.upload.useMutation({ onSuccess: async () => { await utils.album.list.invalidate({ familyGroupId, favoritesOnly }); await utils.album.search.invalidate(); setStatusMessage(t("family.albumUploaded")); toast.success(t("family.albumUploaded")); }, onError: () => { setStatusMessage(t("family.albumUploadFailed")); toast.error(t("family.albumUploadFailed")); } });
  const setFavorite = trpc.album.setFavorite.useMutation({ onSuccess: async () => { await utils.album.list.invalidate({ familyGroupId, favoritesOnly }); await utils.album.search.invalidate(); }, onError: () => { setStatusMessage(t("family.albumFavoriteFailed")); } });
  const sendReaction = trpc.careMessages.create.useMutation({ onSuccess: async () => { await utils.careMessages.list.invalidate({ familyGroupId }); setStatusMessage(t("family.albumReactionSent")); }, onError: () => setStatusMessage(t("family.albumReactionFailed")) });
  const handlePhotoSelect = async (event: React.ChangeEvent<HTMLInputElement>) => { const file = event.target.files?.[0]; event.target.value = ""; if (!file) return; if (!isSupportedAlbumMimeType(file.type)) { toast.error(t("family.albumFileTypeError")); return; } if (file.size > MAX_ALBUM_PHOTO_BYTES) { toast.error(t("family.albumFileSizeError")); return; } try { const dataUrl = await readAsDataUrl(file); await uploadPhoto.mutateAsync({ familyGroupId, dataUrl, fileName: file.name, mimeType: file.type as AlbumMimeType }); } catch { toast.error(t("family.albumUploadFailed")); } };
  const downloadAll = () => { if (displayedPhotos.length === 0) return; displayedPhotos.forEach((photo, index) => { window.setTimeout(() => { const anchor = document.createElement("a"); anchor.href = photo.imageUrl; anchor.download = photo.fileName; anchor.rel = "noopener"; document.body.appendChild(anchor); anchor.click(); anchor.remove(); }, index * 180); }); setStatusMessage(t("family.albumDownloadStarted").replace("{count}", String(displayedPhotos.length))); };
  const emptyMessage = discoveryMode === "search" ? t("family.albumNoResults") : discoveryMode === "favorites" ? t("family.albumNoFavorites") : t("family.albumEmpty");

  return <Card className="border-0 bg-gradient-to-br from-sky-50 via-white to-rose-50 shadow-md"><CardHeader className="gap-3 pb-3 sm:flex-row sm:items-center sm:justify-between"><div><CardTitle className="flex items-center gap-2 text-base text-slate-800"><Sparkles className="h-5 w-5 text-violet-500" aria-hidden="true" />{t("family.albumTitle")}</CardTitle><p className="mt-1 text-xs text-slate-500">{t("family.albumDescription")}</p></div><div className="flex flex-wrap gap-2"><Button size="sm" variant={favoritesOnly ? "default" : "outline"} aria-pressed={favoritesOnly} onClick={() => setFavoritesOnly((current) => !current)}><Heart className={`mr-1.5 h-4 w-4 ${favoritesOnly ? "fill-current" : ""}`} aria-hidden="true" />{t("family.albumFavorites")}</Button><Button size="sm" variant="outline" onClick={downloadAll} disabled={displayedPhotos.length === 0}><Download className="mr-1.5 h-4 w-4" aria-hidden="true" />{t("family.albumDownloadAll")}</Button><Button size="sm" onClick={() => fileInputRef.current?.click()} disabled={uploadPhoto.isPending}>{uploadPhoto.isPending ? <Loader2 className="mr-1.5 h-4 w-4 animate-spin" aria-hidden="true" /> : <ImagePlus className="mr-1.5 h-4 w-4" aria-hidden="true" />}{t("family.albumAdd")}</Button><input ref={fileInputRef} type="file" accept="image/jpeg,image/png,image/webp" className="hidden" aria-label={t("family.albumAdd")} onChange={handlePhotoSelect} /></div></CardHeader><CardContent><div className="mb-4 flex items-center gap-2 rounded-xl border bg-white px-3 py-1.5 shadow-sm"><Search className="h-4 w-4 text-sky-500" aria-hidden="true" /><label className="sr-only" htmlFor="family-album-search">{t("family.albumSearch")}</label><Input id="family-album-search" value={keyword} onChange={(event) => setKeyword(event.target.value)} placeholder={t("family.albumSearch")} className="h-8 border-0 p-0 text-sm shadow-none focus-visible:ring-0" />{keyword && <button type="button" className="rounded p-1 text-slate-400 hover:text-slate-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-sky-600" onClick={() => setKeyword("")} aria-label={t("family.albumClearSearch")}><X className="h-4 w-4" aria-hidden="true" /></button>}</div>{isLoading || isSearching ? <div className="flex min-h-52 items-center justify-center text-sm text-slate-500" role="status" aria-live="polite"><Loader2 className="mr-2 h-4 w-4 animate-spin" aria-hidden="true" />{t("family.albumLoading")}</div> : displayedPhotos.length === 0 ? <div className="flex min-h-52 flex-col items-center justify-center rounded-2xl border border-dashed border-sky-200 bg-white/70 p-8 text-center"><ImagePlus className="mb-3 h-9 w-9 text-sky-300" aria-hidden="true" /><p className="text-sm font-medium text-slate-700">{emptyMessage}</p><p className="mt-1 text-xs text-slate-500">{t("family.albumEmptyHelp")}</p></div> : <div className="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4">{displayedPhotos.map((photo) => { const tags = Array.isArray(photo.tags) ? photo.tags.filter((tag): tag is string => typeof tag === "string") : []; return <article key={photo.id} className="group overflow-hidden rounded-2xl border border-white bg-white shadow-sm transition-shadow hover:shadow-md"><div className="relative aspect-square overflow-hidden bg-slate-100"><img src={photo.imageUrl} alt={photo.description || photo.fileName} className="h-full w-full object-cover transition-transform duration-300 group-hover:scale-105" /><button type="button" aria-label={photo.isFavorite ? t("family.albumRemoveFavorite") : t("family.albumAddFavorite")} onClick={() => setFavorite.mutate({ familyGroupId, photoId: photo.id, isFavorite: !photo.isFavorite })} className="absolute right-2 top-2 rounded-full bg-white/90 p-2 text-rose-500 shadow-sm transition-transform active:scale-95 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-rose-600"><Heart className={`h-4 w-4 ${photo.isFavorite ? "fill-current" : ""}`} /></button></div><div className="space-y-2 p-3"><p className="line-clamp-2 text-xs leading-relaxed text-slate-700">{photo.description || t("family.albumDescriptionPending")}</p>{tags.length > 0 && <div className="flex flex-wrap gap-1">{tags.slice(0, 3).map((tag) => <Badge key={tag} variant="secondary" className="gap-1 text-[10px]"><Tag className="h-2.5 w-2.5" aria-hidden="true" />{tag}</Badge>)}</div>}<div className="flex flex-wrap items-center gap-x-3 gap-y-1"><a href={photo.imageUrl} download={photo.fileName} className="inline-flex items-center text-[11px] font-medium text-sky-600 hover:text-sky-700"><Download className="mr-1 h-3 w-3" aria-hidden="true" />{t("family.albumSave")}</a><button type="button" className="inline-flex items-center rounded text-[11px] font-medium text-rose-600 hover:text-rose-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-rose-600" disabled={sendReaction.isPending} onClick={() => sendReaction.mutate({ familyGroupId, message: t("family.albumReactionMessage") })}><MessageCircleHeart className="mr-1 h-3 w-3" aria-hidden="true" />{t("family.albumReaction")}</button></div></div></article>; })}</div>}<p className="mt-3 min-h-5 text-center text-xs font-medium text-sky-700" role="status" aria-live="polite">{statusMessage}</p></CardContent></Card>;
}
